# What this thing do

Every access on http://host will be redirect to https://host 

Example:

Presuming you have two sites site-a.com and site-b.com, both will be redirect to https by your load balancer if this containers left placed behind them. 
